import React from 'react';
import ReactDOM from 'react-dom';
import App from "./App"; //How we use CSS 

// New Date(Year, Month, Day, Hours, Minutes, Seconds)

ReactDOM.render(
  <>
     <App></App>
  </>,
  document.getElementById('root')
);
